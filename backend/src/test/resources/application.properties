spring.application.name=acc-management-system
spring.threads.virtual.enabled=true

# In-memory H2 for tests; kept open for the JVM lifetime of the test run.
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Liquibase owns the schema (same changelogs as runtime)
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.xml

# httpstat.us client (test defaults — no real network calls are made in tests)
httpstat.base-url=https://httpstat.us
httpstat.status=200
httpstat.connect-timeout=2s
httpstat.read-timeout=3s
